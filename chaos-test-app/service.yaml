apiVersion: v1
kind: Service
metadata:
  name: httpbin
  namespace: chaos-test
  labels:
    app: httpbin
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: httpbin
